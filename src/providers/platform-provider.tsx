import { createContext, useContext, type ReactNode } from "react"; import type { PlatformContracts } from "../contracts/platform"; const PlatformContext=createContext<PlatformContracts>({}); export function EdjavuPlatformProvider({children,...contracts}:{children:ReactNode}&PlatformContracts){return <PlatformContext.Provider value={contracts}>{children}</PlatformContext.Provider>} export function usePlatform(){return useContext(PlatformContext)}