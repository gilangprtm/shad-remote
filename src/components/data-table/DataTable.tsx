import type { ReactNode } from "react"; export type DataColumn<T>={key:string;header:string;cell?:(row:T)=>ReactNode}; export function DataTable<T extends Record<string,unknown>>({data,columns,emptyMessage="No records found",onRowClick}:{data:T[];columns:DataColumn<T>[];emptyMessage?:string;onRowClick?:(row:T)=>void}){return <div className="edjavu-table-wrap"><table className="edjavu-table"><thead><tr>{columns.map(c=><th key={c.key}>{c.header}</th>)}</tr></thead><tbody>{data.length?data.map((row,i)=><tr key={i} onClick={()=>onRowClick?.(row)} tabIndex={onRowClick?0:-1}>{columns.map(c=><td key={c.key}>{c.cell?c.cell(row):String(row[c.key]??"")}</td>)}</tr>):<tr><td colSpan={columns.length} className="edjavu-empty">{emptyMessage}</td></tr>}</tbody></table></div>}